// other emblem inport should i use?if decided add it to the import list
//  import ChevronsRight from './Icons/ChevronsRight';

const Button = ({ children }) => {
    return (
        <button className="btn">
        {children}
        </button>
    );
    }

export default Button;
